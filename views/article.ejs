<section class="mb-8">
  <h1 class="text-3xl md:text-4xl font-bold tracking-tight mb-2"><%= article.title %></h1>
  <% if (article.date) { %>
    <p class="text-sm text-slate-500"><%= new Date(article.date).toLocaleDateString() %></p>
  <% } %>
  <% if (article.tags && article.tags.length) { %>
    <div class="mt-3 flex flex-wrap gap-1.5">
      <% article.tags.forEach(tag => { %>
        <span class="inline-block rounded-full bg-slate-100 text-slate-700 text-xs px-2 py-1"><%= tag %></span>
      <% }) %>
    </div>
  <% } %>
</section>

<section>
  <div class="max-w-none text-slate-800 leading-relaxed space-y-4">
    <%- content %>
  </div>
</section>
